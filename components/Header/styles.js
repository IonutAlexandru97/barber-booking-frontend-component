const React = require("react-native");
const { StyleSheet } = React;

export default {
    header: {
        fontSize: 20,
        fontWeight: "800",
        marginBottom: 10,
        textAlign: 'center',
      }
}