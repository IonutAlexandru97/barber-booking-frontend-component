const React = require("react-native");
const { StyleSheet } = React;

export default {
    background: {
        flex: 1,
        width: '100%',
      },
      container: {
        flex: 1,
        padding: 20,
        width: '100%',
        maxWidth: 340,
        alignSelf: 'center',
        alignItems: 'center',
        justifyContent: 'center',
      },
}